package com.example.brickapp.ui.theme

import androidx.compose.ui.graphics.Color

// App brand colors
val Teal700 = Color(0xFF00796B)
val Teal500 = Color(0xFF009688)
val Teal200 = Color(0xFF80CBC4)
val TealDark = Color(0xFF004D40)

val BlueGrey700 = Color(0xFF455A64)
val BlueGrey500 = Color(0xFF607D8B)
val BlueGrey200 = Color(0xFFB0BEC5)

// Earthquake magnitude color scale (color + numeral — never color alone)
val MagnitudeMinor = Color(0xFF4CAF50)     // M < 3.0 — green
val MagnitudeLight = Color(0xFFFFC107)     // M 3.0–3.9 — amber
val MagnitudeModerate = Color(0xFFFF9800)  // M 4.0–4.9 — orange
val MagnitudeStrong = Color(0xFFFF5722)    // M 5.0–5.9 — deep orange
val MagnitudeMajor = Color(0xFFF44336)     // M 6.0–6.9 — red
val MagnitudeGreat = Color(0xFF9C27B0)     // M 7.0+ — purple

// PAGER alert level colors
val AlertGreen = Color(0xFF4CAF50)
val AlertYellow = Color(0xFFFFEB3B)
val AlertOrange = Color(0xFFFF9800)
val AlertRed = Color(0xFFF44336)

// Surface variants
val SurfaceDark = Color(0xFF1C1B1F)
val SurfaceLight = Color(0xFFFFFBFE)
